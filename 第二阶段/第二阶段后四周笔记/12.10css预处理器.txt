css预处理器a.scss
1）问题：一个css样式很大，不好维护,每个人代码的风格不一样；
2）优点：可以进行模块化开发
3）编译时按照统一的格式，会合并重复的代码

github
写项目之前，先从远程仓库拉去最新代码

写项目，多add，多commit

写项目中，如果你要修改一个和同时一样的文件，先让同时提交代码，
自己再获取最新代码

如果项目中，你修改了共用模块，需要立马push到远程仓库

项目中某个功能写完了，先add，再commit,再push代码

git branch name创建分支，方便管理项目；比如以人的名字创建分支

git branch 查看分支

git checkout name 进入分支

git checkout -b name 创建分支，并进入分支

git merge合并分支；

当某分支某个模块完成以后，先commit，

